# WO-001-ST-002 当前基线分类与后续范围冻结

## 子任务目标

根据 ST-001 的三视图，把当前文件分类为项目基线、既有验证修复、蓝图控制面或异常项，并冻结后续允许改动。

## 执行顺序

1. 读取 ST-001 报告中的全部文件行。
2. 对照 CONTROL 的当前证据、允许修改总范围和明确非目标。
3. 为每个文件标记项目基线、既有修复、控制面或异常。
4. 核对旧 WO-005 的 `settings.py` 登记遗漏已由本蓝图明确纳入，而不是继续引用旧工单。
5. 发现异常项立即 BLOCKED；全部可分类则更新报告和状态。

## 前置条件

- WO-001-ST-001 完成且三视图证据完整。

## 输入

- WO-001-ST-001 报告。
- 新 `ai/CONTROL.md`。
- 当前项目文件，仅作只读核对。

## 输出

- 当前基线分类表。
- 后续唯一允许变化的六个模块/测试文件和两份控制面文件清单。

## 依赖

- WO-001-ST-001。

## 改动坐标

- 仅执行状态与最新报告。

## 允许文件

- `ai/STATE.md`
- `ai/LATEST-REPORT.md`

## 禁止文件

- 全部源码、测试、依赖、配置、文档、Git metadata、CONTROL 和执行协议。

## 精确实施要求

- 旧报告和旧 WO-005 只作为历史背景，不作为当前授权来源。
- 当前用户要求生成的新 CONTROL 是本轮范围来源；不得再声称“待审查接受 WO-005”。
- 不以文件合理、测试通过或 mtime 代替分类。
- 后续源码修改只在对应 WO-002 ST 的测试先失败时允许。

## 完成定义

- 所有当前文件均可分类；后续允许坐标冻结且无第三类。

## 验收标准

- 分类数量与 ST-001 三视图逐文件集合一致。
- 报告明确列出异常项为零，或精确阻塞路径。

## 允许执行命令

- 无。

## 禁止执行命令

- 所有终端、Git、测试、构建、服务、网络和安装命令。

## 验证等级

- R0

## 最大修复轮数

- 0；只读分类，不修文件。

## 最大运行时间

- 120 秒文件级核对预算；不执行命令。

## CPU 和内存风险

- 低；只读文本核对，无网络和后台进程。

## 失败处理方式

- 任一文件无法分类即 BLOCKED，并在报告写明路径和所需人工判断。

## HARD STOP 条件

- ST-001 证据缺行、集合无法复算、出现构建产物或需要扩大新 CONTROL 范围。
